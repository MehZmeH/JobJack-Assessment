var express = require("express");
const router = express.Router();

router.post(`/directory/exists`);
//BOOKMARK: Was busy here with using URL parameters for the get functionality.
router.post(`/directory/`);
router.post(`/directory/exists`);
router.post(`/directory/exists`);

module.exports = router;
